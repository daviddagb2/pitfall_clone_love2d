function love.conf(t)
    t.title = "Pitfall clone"		    -- The title of the window the game is in (string)
    t.window.width = 160			    -- The window width (number)
    t.window.height = 192			    -- The window height (number)
    t.window.fullscreen = false		-- Enable fullscreen (boolean)
    t.window.vsync = false			    -- Enable vertical sync (boolean)
    t.console = true
end